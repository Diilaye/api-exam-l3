# Variables d'environnement - EXEMPLE
# Copier ce fichier vers .env et modifier les valeurs

# Port du serveur
PORT=3000

# URL de connexion MongoDB
# Pour MongoDB local :
MONGODB_URI=mongodb://localhost:27017/api-exam

# Pour MongoDB Atlas (remplacer les valeurs) :
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# Clé secrète pour JWT (OBLIGATOIRE : changer en production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters

# Durée de validité du token JWT
JWT_EXPIRE=7d

# Environnement (development, production)
NODE_ENV=development